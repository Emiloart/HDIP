{
  "name": "hdip",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "bash scripts/validate-web.sh lint",
    "typecheck": "bash scripts/validate-web.sh typecheck",
    "test": "bash scripts/validate-web.sh test",
    "schema:validate": "node scripts/validate-schemas.mjs",
    "validate:web": "bash scripts/validate-web.sh"
  },
  "devDependencies": {
    "ajv": "^8.18.0",
    "ajv-formats": "^3.0.1"
  }
}
